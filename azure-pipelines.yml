trigger: none

pool: App   # tumhara self-hosted pool

steps:
- task: PowerShell@2
  displayName: 'Install Node Packages (npm install)'
  inputs:
    targetType: 'inline'
    pwsh: false     # 👈 ye important! pwsh error hat jayega
    script: |
      npm install

- task: PowerShell@2
  displayName: 'Build Project (npm run build)'
  inputs:
    targetType: 'inline'
    pwsh: false     # 👈 pwsh ke badle Windows PowerShell use karega
    script: |
      npm run build
